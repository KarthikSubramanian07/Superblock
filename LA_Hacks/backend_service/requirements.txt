# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0

# Data & ML
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.5,<2.0
joblib>=1.4,<2.0
lightgbm>=4.5,<5.0

# ONNX runtime (ZETIC edge node + ALS inference)
onnxruntime>=1.18,<2.0

# Spatial indexing
h3>=4.1,<5.0

# HTTP & config
requests>=2.32,<3.0
python-dotenv>=1.0,<2.0

# Auth
python-jose[cryptography]>=3.3.0

# Database
pymongo>=4.8,<5.0

# Agents
uagents>=0.24,<1.0

# Model export (training scripts only — skl2onnx omitted due to Windows path length limit)
